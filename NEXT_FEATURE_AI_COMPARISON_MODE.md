# AI比較モード実装計画

## 概要
現在は「精度評価モード」（元画像 vs AI処理結果）のみ実装済み。
「AI比較モード」（AI結果1 vs AI結果2）を追加する。

## 現状

### Aモード（精度評価）: ✅ 実装済み
- 元画像 vs AI処理結果1
- 元画像 vs AI処理結果2
- 元画像 vs AI処理結果3...
- → 各AI結果の**精度**を元画像と比較して評価

### Bモード（AI比較）: ❌ 未実装
- AI処理結果1 vs AI処理結果2
- → どちらの超解像が**より良いか**を直接比較
- 元画像があれば精度も評価可能

## 推奨実装方法：オプション2（動的UI）

### 理由
- **直感的**: UIが状況に応じて変化するので誤操作を防げる
- **バリデーション**: 必須フィールドが明確
- **柔軟**: 将来的に3つ以上の比較も可能

### UI設計

```
┌─────────────────────────────────┐
│ 📊 比較モード                    │
├─────────────────────────────────┤
│ ● 精度評価（元画像基準）         │
│ ○ AI比較（直接対決）            │
└─────────────────────────────────┘

[精度評価モード選択時]
┌─────────────────────────────────┐
│ 📷 元画像（必須）                │
│ [選択] /path/to/original.png    │
├─────────────────────────────────┤
│ 🤖 AI処理結果1（必須）          │
│ [選択] /path/to/ai1.png         │
├─────────────────────────────────┤
│ 🤖 AI処理結果2（任意）          │
│ [選択]                          │
└─────────────────────────────────┘

[AI比較モード選択時]
┌─────────────────────────────────┐
│ 元画像フィールド非表示           │
├─────────────────────────────────┤
│ 🤖 AI結果A（必須）              │
│ [選択] /path/to/ai1.png         │
├─────────────────────────────────┤
│ 🤖 AI結果B（必須）              │
│ [選択] /path/to/ai2.png         │
└─────────────────────────────────┘
```

## 実装手順

### 1. modern_gui.py に比較モード選択を追加
```python
# 評価設定セクション内に追加
self.comparison_mode = tk.StringVar(value="accuracy")  # "accuracy" or "ai_vs_ai"

# ラジオボタン
accuracy_mode = ctk.CTkRadioButton(
    ...,
    text="精度評価（元画像基準）",
    variable=self.comparison_mode,
    value="accuracy",
    command=self.update_ui_for_mode
)

ai_compare_mode = ctk.CTkRadioButton(
    ...,
    text="AI比較（直接対決）",
    variable=self.comparison_mode,
    value="ai_vs_ai",
    command=self.update_ui_for_mode
)
```

### 2. UIを動的に変更するメソッド追加
```python
def update_ui_for_mode(self):
    """比較モードに応じてUIを変更"""
    mode = self.comparison_mode.get()

    if mode == "accuracy":
        # 元画像フィールド表示
        self.original_frame.pack(...)
        # ラベル変更
        self.img1_label.configure(text="AI処理結果1（必須）")
    else:  # ai_vs_ai
        # 元画像フィールド非表示
        self.original_frame.pack_forget()
        # ラベル変更
        self.img1_label.configure(text="AI結果A（必須）")
        self.img2_label.configure(text="AI結果B（必須）")
```

### 3. バリデーション追加
```python
def validate_inputs(self):
    """モードに応じた入力チェック"""
    mode = self.comparison_mode.get()

    if mode == "accuracy":
        if not self.original_path.get():
            messagebox.showerror("エラー", "元画像を選択してください")
            return False
        if not self.img1_path.get():
            messagebox.showerror("エラー", "AI処理結果を選択してください")
            return False
    else:  # ai_vs_ai
        if not self.img1_path.get() or not self.img2_path.get():
            messagebox.showerror("エラー", "2つのAI結果を選択してください")
            return False

    return True
```

### 4. analyze_images呼び出しを分岐
```python
def run_analysis(self):
    mode = self.comparison_mode.get()

    if mode == "accuracy":
        # 元画像 vs AI処理結果
        results = analyze_images(
            self.original_path.get(),  # 元画像
            self.img1_path.get(),      # AI処理結果
            self.output_dir.get(),
            None,
            self.evaluation_mode.get()
        )
    else:  # ai_vs_ai
        # AI結果1 vs AI結果2
        results = analyze_images(
            self.img1_path.get(),   # AI結果A
            self.img2_path.get(),   # AI結果B
            self.output_dir.get(),
            None,  # 元画像なし（または任意）
            self.evaluation_mode.get()
        )
```

## 注意点

1. **advanced_image_analyzer.py は修正不要**
   - 既に2つの画像を比較する汎用実装になっている
   - img1_path, img2_pathに何を渡すかでモードが決まる

2. **comparison_reportのラベル**
   - 現在: 「元画像」「AI処理結果」
   - AI比較モード時: 「AI結果A」「AI結果B」に動的変更が必要

3. **後方互換性**
   - 既存のコードは動作を維持
   - デフォルトは「精度評価モード」

## 将来的な拡張

- 3つ以上のAI結果を一度に比較
- 元画像ありのAI比較モード（精度も表示）
- バッチモードでのAI比較

## 参考リンク

- modern_gui.py: 305-370行（評価設定セクション）
- advanced_image_analyzer.py: 979-1009行（analyze_images関数）

---

作成日: 2025-11-02
次回実装予定
